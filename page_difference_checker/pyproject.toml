[tool.poetry]
name = "page_difference_checker"
version = "0.1.0"
description = ""
authors = ["The Author"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
beautifulsoup4 = ">=4.12.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
